<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <permission
        android:name="com.rahul100ni.tether.permission.START_MONITORING"
        android:protectionLevel="signature" />

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Exact alarms for scheduled blocking: SCHEDULE_EXACT_ALARM covers Android 12/12L,
         USE_EXACT_ALARM (auto-granted) covers 13+ -->
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <uses-permission android:name="android.permission.NFC" />

    <uses-feature
        android:name="android.hardware.nfc"
        android:required="false" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tether">
        <activity
            android:name=".NfcWriteActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Tether">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>
        <activity
            android:name=".NfcHandlerActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
                <!-- Must match NfcWriteActivity.NFC_MIME_TYPE -->
                <data android:mimeType="application/vnd.com.rahul100ni.tether" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AppSelectionActivity"
            android:exported="false"
            android:label="@string/title_activity_app_selection"
            android:theme="@style/Theme.Tether" />
        <activity
            android:name=".BlockingActivity"
            android:exported="false"
            android:label="@string/title_activity_blocking"
            android:launchMode="singleInstance"
            android:taskAffinity=""
            android:theme="@style/Theme.Tether" />
        <activity
            android:name=".ShortcutHandlerActivity"
            android:exported="true"
            android:label="@string/title_activity_shortcut_handler"
            android:permission="com.rahul100ni.tether.permission.START_MONITORING"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <activity
            android:name=".QrCodeActivity"
            android:exported="false"
            android:label="@string/title_activity_qr_code"
            android:theme="@style/Theme.Tether" />
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/title_activity_settings"
            android:theme="@style/Theme.Tether" />
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:screenOrientation="portrait"
            tools:replace="screenOrientation" />

        <service
            android:name=".AppMonitoringService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Monitors app usage to provide blocking functionality." />
        </service>

        <receiver
            android:name=".BootCompletedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Exported so automation apps (Tasker, MacroDroid, Samsung Routines) can start
             or stop sessions; external triggers are ignored unless the user enables
             "Allow automation apps" in Settings. Also the target of schedule alarms. -->
        <receiver
            android:name=".ScheduleReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.rahul100ni.tether.SCHEDULE_START" />
                <action android:name="com.rahul100ni.tether.SCHEDULE_STOP" />
            </intent-filter>
        </receiver>
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

</manifest>

